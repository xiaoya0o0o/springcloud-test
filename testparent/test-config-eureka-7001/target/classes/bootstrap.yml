spring:
  cloud:
    config:
      name: test-config-eureka-client     #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev
      label: master
      uri: http://config3322.com:3322      #SpringCloudConfig获取的服务地址